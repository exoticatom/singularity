# singularity — .gitignore
#
# Project: singularity
# Purpose: Prevents sensitive and generated files from being committed
#          to the repository. secrets.yaml must NEVER be tracked by Git.

# ESPHome secrets — contains Wi-Fi credentials, API keys, OTA passwords
secrets.yaml

# ESPHome build artifacts
.esphome/

# macOS
.DS_Store
